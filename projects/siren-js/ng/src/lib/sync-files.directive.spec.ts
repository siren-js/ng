import { SyncFilesDirective } from './sync-files.directive';

describe('SyncFilesDirective', () => {
  it('should create an instance', () => {
    const directive = new SyncFilesDirective();
    expect(directive).toBeTruthy();
  });
});
